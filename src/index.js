//program for a simple calculator
//take an operator
var firstNumber=prompt("Enter the firstNumber")
var secondNumber=prompt("Enter the secondNumber")
//result using declaration of function
//program to add two numbers
function add(fisrtNumber,secondNumber) {
  return firstNumber+secondNumber;
  alert((result = add(Number(firstNumber),Number(secondNumber)));
}
//result using declaration of function
//program to subtract two numbers
function subtract(fisrtNumber,secondNumber) {
  return firstNumber-secondNumber;
  alert((result = add(Number(firstNumber),Number(secondNumber)));
}
//result using declaration of function
//program to add two numbers
function division(fisrtNumber,secondNumber) {
  return firstNumber/secondNumber;
  alert((result = divide(Number(firstNumber),Number(secondNumber)));
}
//result using declaration of function
//program to add two numbers
function multiply(fisrtNumber,secondNumber) {
  return firstNumber*secondNumber;
  alert((result = multiply(Number(firstNumber),Number(secondNumber)));
}
